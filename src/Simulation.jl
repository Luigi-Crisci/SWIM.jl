

function simulate(canvas::Canvas,nodes::Array{Node},seconds::Int,delta::Int)
	time = DateTime(0)
	# Start simulation
	while Dates.second(time) < seconds
	#TODO: Update seen for the cells
	#TODO: Add events here		
	end
end